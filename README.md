# We Did Dat Try-On (Barber Only)

Installable web app (PWA-style) for Android phone + Samsung tablet later.

## Run locally
1. Install Node.js (LTS)
2. In this folder:
   - `npm install`
   - `npm run dev`

## Build
- `npm run build` (output goes to `dist/`)
